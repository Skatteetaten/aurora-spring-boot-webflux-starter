spring:
    application:
        name: webflux-starter
    sleuth:
        otel:
            log:
                slf4j:
                    enabled: true
        baggage:
            correlation-fields: Korrelasjonsid,Klientid
            local-fields: Korrelasjonsid,Klientid
            remote-fields: Korrelasjonsid,Klientid
aurora:
    webflux:
        header:
            webclient:
                interceptor:
                    enabled: true

openshift:
    cluster: local

pod:
    name: test-123
    namespace: test-dev

server:
    shutdown: immediate

management:
    server:
        port: -1

logging.level.zipkin2.reporter: ERROR
logging.level.no.skatteetaten.aurora.webflux: DEBUG